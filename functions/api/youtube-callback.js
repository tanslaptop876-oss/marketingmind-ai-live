import {apiHeaders,baseUrl} from '../_shared/supabase.js';
import {open,seal} from '../_shared/provider-crypto.js';
const json=(body,status=200,headers={})=>Response.json(body,{status,headers:{'cache-control':'private, no-store','x-content-type-options':'nosniff',...headers}}),cookies=request=>Object.fromEntries(String(request.headers.get('cookie')||'').split(';').map(value=>value.trim().split(/=(.*)/s)).filter(parts=>parts[0]));
export async function onRequestGet(context){
  const url=new URL(context.request.url),clear='mm_youtube_oauth=; Path=/api; HttpOnly; Secure; SameSite=Lax; Max-Age=0';let oauth;
  try{oauth=await open(decodeURIComponent(cookies(context.request).mm_youtube_oauth||''),context.env.PROVIDER_TOKEN_ENCRYPTION_KEY)}catch{return json({ok:false,error:'invalid_oauth_state'},403,{'set-cookie':clear})}
  if(!url.searchParams.get('state')||oauth.state!==url.searchParams.get('state')||oauth.exp<Date.now())return json({ok:false,error:'invalid_oauth_state'},403,{'set-cookie':clear});const code=url.searchParams.get('code');if(!code)return json({ok:false,error:'missing_authorization_code'},400,{'set-cookie':clear});
  try{const tokenResponse=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:context.env.GOOGLE_CLIENT_ID,client_secret:context.env.GOOGLE_CLIENT_SECRET,redirect_uri:context.env.YOUTUBE_REDIRECT_URI,grant_type:'authorization_code',code})}),token=await tokenResponse.json();if(!tokenResponse.ok||!token.access_token)throw new Error('token_exchange_failed');const channelResponse=await fetch('https://www.googleapis.com/youtube/v3/channels?part=id%2Csnippet&mine=true',{headers:{authorization:`Bearer ${token.access_token}`}}),channelData=await channelResponse.json();if(!channelResponse.ok)throw new Error('channel_discovery_failed');const channels=(channelData.items||[]).map(item=>({id:item.id,title:item.snippet?.title||'YouTube channel',customUrl:item.snippet?.customUrl||null}));if(!channels.length)throw new Error('youtube_channel_required');const encrypted=await seal({refreshToken:token.refresh_token||null,accessToken:token.access_token,expiresAt:Date.now()+Number(token.expires_in||3600)*1000,channels},context.env.PROVIDER_TOKEN_ENCRYPTION_KEY),response=await fetch(`${baseUrl(context.env)}/rest/v1/mm_provider_connections?on_conflict=owner_id,provider`,{method:'POST',headers:{...apiHeaders({SUPABASE_ANON_KEY:context.env.SUPABASE_SERVICE_ROLE_KEY},context.env.SUPABASE_SERVICE_ROLE_KEY),prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({owner_id:oauth.userId,provider:'youtube',encrypted_credentials:encrypted,account_summary:channels,updated_at:new Date().toISOString()})});if(!response.ok)throw new Error('secure_storage_failed');return new Response(null,{status:302,headers:{location:'/#scheduler','cache-control':'private, no-store','set-cookie':clear}})}catch(error){return json({ok:false,error:error.message||'youtube_connection_failed'},502,{'set-cookie':clear})}
}
export function onRequest(){return json({ok:false,error:'method_not_allowed'},405)}

